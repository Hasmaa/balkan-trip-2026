import { MapPin, Info } from 'lucide-react'; import type { Resource } from '../types/trip';
export function ResourceCard({resource,onOpen}:{resource:Resource;onOpen:(r:Resource)=>void}) { return <article className="resource-card"><div><p className="eyebrow">{resource.country} · {resource.type.replace('-', ' ')}</p><h3>{resource.name}</h3><p>{resource.shortDescription}</p></div><div className="card-footer"><span className={`status ${resource.verificationStatus}`}>{resource.verificationStatus === 'official' ? 'Official source' : resource.verificationStatus === 'secondary' ? 'Secondary source' : 'Needs reconfirmation'}</span><button onClick={()=>onOpen(resource)} aria-label={`Open details for ${resource.name}`}><Info size={16}/>Details</button></div><a href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(resource.mapsQuery)}`} target="_blank" rel="noopener noreferrer"><MapPin size={15}/> Maps</a></article> }
