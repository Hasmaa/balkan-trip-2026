import type { TripDay } from '../types/trip'; import { dateForDay, formatTripDate } from '../lib/trip'; export function PrintItinerary({days,start}:{days:TripDay[];start:string}){return <section className="print-itinerary section"><div className="section-heading"><p className="eyebrow">Print itinerary</p><h2>A clean version for the glovebox.</h2><button onClick={()=>window.print()}>Print / save as PDF</button></div><ol>{days.map(d=><li key={d.day}><b>{formatTripDate(dateForDay(start,d.day),true)} · {d.title}</b><span>day {d.day} · {d.suggestedDeparture} · approx. {Math.floor(d.approximateDriveMinutes/60)}h · {d.summary}</span></li>)}</ol></section>}
