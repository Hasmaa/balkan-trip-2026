import { Car, CloudSun, Share2, Printer } from 'lucide-react'; import type { TripDay } from '../types/trip'; import { directionsUrl, weatherUrl } from '../lib/trip';
export function DayQuickActions({day}:{day:TripDay}) { const share=()=>navigator.clipboard?.writeText(`${location.origin}${location.pathname}?day=${day.day}`); return <div className="day-actions"><a href={directionsUrl(day.origin,day.destination,day.waypointNames)} target="_blank" rel="noopener noreferrer"><Car/>Open day route</a><a href={weatherUrl(day.destination)} target="_blank" rel="noopener noreferrer"><CloudSun/>Weather</a><button onClick={()=>window.print()}><Printer/>Print this day</button><button onClick={share}><Share2/>Copy share link</button></div> }
