import { countries, countryOfDay } from '../data/countries';
export function CountryProgress({day}:{day:number}) { const context=countryOfDay(day); const origin=countries[context.origin],destination=countries[context.destination],crossing=context.origin!==context.destination; return <div className="country-progress" aria-label={`Day ${day}: ${origin.name}${crossing?` to ${destination.name}`:''}`}><span className="country-now">{destination.flag} <b>{destination.name}</b></span>{crossing?<span className="country-border">Border today: {origin.flag} {origin.name} → {destination.flag} {destination.name}</span>:<span>{destination.currency} · {destination.drivingNote}</span>}</div> }
