import { useEffect, useRef, useState } from 'react'; import maplibregl, { type Map } from 'maplibre-gl'; import 'maplibre-gl/dist/maplibre-gl.css'; import type { Resource, TripDay } from '../types/trip';
const style=import.meta.env.VITE_MAP_STYLE_URL || 'https://tiles.openfreemap.org/styles/liberty';
export function OverviewMap({days,resources,selected,onSelectDay,onOpen}:{days:TripDay[];resources:Resource[];selected:number;onSelectDay:(n:number)=>void;onOpen:(r:Resource)=>void}) {const el=useRef<HTMLDivElement>(null),map=useRef<Map|null>(null);const [failed,setFailed]=useState(false); useEffect(()=>{if(!el.current||map.current)return;try {const instance=new maplibregl.Map({container:el.current,style,center:[20.3,42.6],zoom:5});map.current=instance;instance.on('load',()=>{days.forEach(d=>{instance.addSource(`route-${d.day}`,{type:'geojson',data:{type:'Feature',properties:{},geometry:{type:'LineString',coordinates:d.routeCoordinates}}});instance.addLayer({id:`route-${d.day}`,type:'line',source:`route-${d.day}`,paint:{'line-color':d.day===selected?'#e0ad56':'#5c8f83','line-width':d.day===selected?5:2.5,'line-opacity':d.day===selected?1:.48}})}); resources.filter(r=>r.type==='campground').forEach(r=>{const marker=document.createElement('button');marker.className='map-marker';marker.textContent=String(r.dayIds[0]);marker.ariaLabel=`Open ${r.name}`;marker.onclick=()=>onOpen(r);new maplibregl.Marker({element:marker}).setLngLat(r.coordinates).addTo(instance)});const bounds=new maplibregl.LngLatBounds();days.flatMap(d=>d.routeCoordinates).forEach(p=>bounds.extend(p));instance.fitBounds(bounds,{padding:48,maxZoom:7});});instance.on('error',()=>setFailed(true));}catch{setFailed(true)}return()=>{map.current?.remove();map.current=null}},[days,resources,onOpen,selected]);useEffect(()=>{const instance=map.current;if(!instance||!instance.isStyleLoaded())return;days.forEach(d=>{if(instance.getLayer(`route-${d.day}`))instance.setPaintProperty(`route-${d.day}`,'line-color',d.day===selected?'#e0ad56':'#5c8f83');if(d.day===selected){const b=new maplibregl.LngLatBounds();d.routeCoordinates.forEach(p=>b.extend(p));instance.fitBounds(b,{padding:72,maxZoom:8,duration:700})}})},[selected,days]);if(failed)return <div className="map-fallback"><b>Map unavailable</b><p>The planned route is still available:</p><ol>{days.map(d=><li key={d.day}><button onClick={()=>onSelectDay(d.day)}>Day {d.day}: {d.title}</button></li>)}</ol></div>;return <div className="map-wrap"><div ref={el} className="map" aria-label="Interactive map of the Balkan road trip"/><div className="map-overlay"><b>Route layers</b><span>● Selected day</span><span>● Camps</span><small>Map data is planning-only; verify road conditions.</small></div></div> }
